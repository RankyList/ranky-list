name: Code testing

on: [pull_request]

jobs:
  playwright:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Start PocketBase
        run: make pocketbase

      - name: Run Playwright
        run: make ci-playwright

  vitest:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run Vitest
        run: make ci-vitest
